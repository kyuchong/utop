fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==12.0
paramiko==3.4.0
anthropic>=0.40.0
pydantic==2.8.0
python-multipart==0.0.9
aiofiles==23.2.1
# 1.0.0 에 고정돼 있었는데 markitdown 이 더 최신을 요구해 설치가 통째로 실패했다.
# 나머지 패키지처럼 정확한 버전으로 고정한다 — 같은 소스면 어디서든 같게 깔려야 한다.
python-dotenv==1.2.2
httpx==0.27.0
netmiko
# SNMP 스텝(get·set·trap). 없으면 스텝이 「pysnmp 미설치」 로 실패한다 —
# 코드가 이미 쓰고 있는데 목록에만 빠져 있었다.
# hlapi.v3arch.asyncio 를 쓰므로 6.x 이상이어야 한다.
pysnmp==7.1.28
python-pptx
Pillow
stcrestclient
asyncpg>=0.30.0
# 워드·PDF·엑셀 등을 마크다운으로 (구현내용 파일 등록)
markitdown[docx,pdf,pptx,xlsx]==0.1.7

# 위키 문서를 **PDF 파일로 바로** 내려받기 위한 것.
#
# 여태는 브라우저 인쇄 창을 띄웠다 — 미리보기가 뜨고 대상을 고르고 저장을
# 눌러야 했다. 게다가 종이가 화면과 자꾸 갈렸다: 인쇄 창이 앱 CSS 를 못
# 불러오거나 옛 판을 들고 가거나.
#
# 서버가 **같은 크롬 엔진**으로 찍으면 그 갈림이 구조적으로 없어진다. 화면을
# 그리는 엔진과 종이를 찍는 엔진이 하나다.
playwright==1.49.1
